<div class="edit">
  <h2>Editing Book</h2>
    <%= form_with model:@book, local:true do |f| %>
<% if @book.errors.any? %>
  <div class="alert">
    <h2 class="alert-title"><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
      <div class="field">
      <%= f.label :title, class: 'label' %>
      <%= f.text_field :title %>
      </div>
      
      <div class="field">
      <%= f.label :body, class: 'label' %>
      <%= f.text_area :body %>
      </div>
      
      <div class="field">
      <%= f.submit 'Update Book' , class: 'edit-btn' %>
      </div>
    <% end %>

    <h5>
    <%= link_to 'Show', book_path(@book.id) %> | <%= link_to 'Back', books_path %>
    </h5>
</div>